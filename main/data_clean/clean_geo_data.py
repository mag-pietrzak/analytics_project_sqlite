import logging
import json

# Setup logging
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')

# Input and output filenames
original_data = 'formatted_zipcodes.json'
output_data = 'formatted_and_cleaned_zipcodes.json'

